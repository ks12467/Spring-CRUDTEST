server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/CRUDTEST?serverTimezone=Asia/Seoul
    username: root
    password: gustn12467


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

